# Matteo's TMUX config

Originally from [Nico](github.com/nviennot/tmux-config).

To install:

```sh
git submodule update --init --merge && \
ln -s "${PWD}/tmux.conf" ~/.tmux.conf && \
mkdir -p ~/.tmux/ && \
ln -s "${PWD}/plugins" ~/.tmux/plugins
```
